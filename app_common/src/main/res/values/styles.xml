<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@style/Animation</item>
        <!--<item name="android:windowIsTranslucent">true</item>-->
    </style>

    <style name="AppSplashTheme" parent="android:Theme.Light.NoTitleBar.Fullscreen">
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@style/Animation</item>
    </style>

    <style name="AppLandSplashTheme" parent="android:Theme.Light.NoTitleBar.Fullscreen">
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@style/Animation</item>
        <!--<item name="android:windowIsTranslucent">true</item>-->
    </style>

    <style name="Animation">
        <item name="android:activityOpenEnterAnimation">@null</item>
        <item name="android:activityOpenExitAnimation">@null</item>
        <item name="android:activityCloseEnterAnimation">@null</item>
        <item name="android:activityCloseExitAnimation">@null</item>
        <item name="android:taskOpenEnterAnimation">@null</item>
        <item name="android:taskOpenExitAnimation">@null</item>
        <item name="android:taskCloseEnterAnimation">@null</item>
        <item name="android:taskCloseExitAnimation">@null</item>
        <item name="android:taskToFrontEnterAnimation">@null</item>
        <item name="android:taskToFrontExitAnimation">@null</item>
        <item name="android:taskToBackEnterAnimation">@null</item>
        <item name="android:taskToBackExitAnimation">@null</item>
    </style>

    <style name="item_container">
        <item name="android:padding">@dimen/content_item_padding</item>
    </style>

    <style name="title">
        <item name="android:textSize">17sp</item>
    </style>

    <style name="content">
        <item name="android:textSize">14sp</item>
    </style>

    <style name="tag">
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/tag_font</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="reply">
        <item name="android:minWidth">50dp</item>
        <item name="android:textColor">@color/reply_font</item>
    </style>
</resources>
